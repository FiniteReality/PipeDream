var/raw_verbatim = @{"strings look like this."}
var/they = @{"
have
multiline
support too"}
var/and = @{"you can use "quotes" and [brackets] in them too!"}
var/escapes = @{"are ignored \"fine" so it's pretty much WYSIWYG"}
