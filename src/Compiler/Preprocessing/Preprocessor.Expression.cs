using PipeDream.Compiler.Parsing.Tree;

namespace PipeDream.Compiler.Preprocessing;

public partial struct Preprocessor
{
    private ExpressionNode? HandleExpression(ExpressionNode expression)
        => expression;
}